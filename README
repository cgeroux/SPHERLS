If this is just the raw source:

autoreconf --install
./configure
make

If this is a package:

To build and install:

./configure
make
make install

To find out more on how to use the SPHERLS packages see user manuals in ./docs/manuals